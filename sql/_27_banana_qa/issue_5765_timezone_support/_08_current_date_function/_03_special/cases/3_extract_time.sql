set timezone 'America/New_York';
select if(extract(year from current_time)=year(current_time),'ok','nok');
select if(extract(year from current_time())=year(current_time()),'ok','nok');
select if(extract(year from curtime())=year(curtime()),'ok','nok');
select if(extract(year from sys_time)=year(sys_time),'ok','nok');
select if(extract(year from systime)=year(systime),'ok','nok');


select if(extract(month from current_time)=month(current_time),'ok','nok');
select if(extract(month from current_time())=month(current_time()),'ok','nok');
select if(extract(month from curtime())=month(curtime()),'ok','nok');
select if(extract(month from systime)=month(systime),'ok','nok');
select if(extract(month from sys_time)=month(sys_time),'ok','nok');


select if(extract(day from current_time)=day(current_time),'ok','nok');
select if(extract(day from current_time())=day(current_time()),'ok','nok');
select if(extract(day from curtime())=day(curtime()),'ok','nok');
select if(extract(day from systime)=day(systime),'ok','nok');
select if(extract(day from sys_time)=day(sys_time),'ok','nok');


select if(extract(hour from current_time)=hour(current_time),'ok','nok');
select if(extract(hour from current_time())=hour(current_time()),'ok','nok');
select if(extract(hour from curtime())=hour(curtime()),'ok','nok');
select if(extract(hour from systime)=hour(systime),'ok','nok');
select if(extract(hour from sys_time)=hour(sys_time),'ok','nok');

select if(extract(minute from current_time)=minute(current_time),'ok','nok');
select if(extract(minute from current_time())=minute(current_time()),'ok','nok');
select if(extract(minute from curtime())=minute(curtime()),'ok','nok');
select if(extract(minute from systime)=minute(systime),'ok','nok');
select if(extract(minute from sys_time)=minute(sys_time),'ok','nok');

select if(extract(second from current_time)=second(current_time),'ok','nok');
select if(extract(second from current_time())=second(current_time()),'ok','nok');
select if(extract(second from curtime())=second(curtime()),'ok','nok');
select if(extract(second from systime)=second(systime),'ok','nok');
select if(extract(second from sys_time)=second(sys_time),'ok','nok');

set timezone '+08:00';
select if(extract(year from current_time)=year(current_time),'ok','nok');
select if(extract(year from current_time())=year(current_time()),'ok','nok');
select if(extract(year from curtime())=year(curtime()),'ok','nok');
select if(extract(year from systime)=year(systime),'ok','nok');
select if(extract(year from sys_time)=year(sys_time),'ok','nok');


select if(extract(month from current_time)=month(current_time),'ok','nok');
select if(extract(month from current_time())=month(current_time()),'ok','nok');
select if(extract(month from curtime())=month(curtime()),'ok','nok');
select if(extract(month from systime)=month(systime),'ok','nok');
select if(extract(month from sys_time)=month(sys_time),'ok','nok');


select if(extract(day from current_time)=day(current_time),'ok','nok');
select if(extract(day from current_time())=day(current_time()),'ok','nok');
select if(extract(day from curtime())=day(curtime()),'ok','nok');
select if(extract(day from systime)=day(systime),'ok','nok');
select if(extract(day from sys_time)=day(sys_time),'ok','nok');


select if(extract(hour from current_time)=hour(current_time),'ok','nok');
select if(extract(hour from current_time())=hour(current_time()),'ok','nok');
select if(extract(hour from curtime())=hour(curtime()),'ok','nok');
select if(extract(hour from systime)=hour(systime),'ok','nok');
select if(extract(hour from sys_time)=hour(sys_time),'ok','nok');

select if(extract(minute from current_time)=minute(current_time),'ok','nok');
select if(extract(minute from current_time())=minute(current_time()),'ok','nok');
select if(extract(minute from curtime())=minute(curtime()),'ok','nok');
select if(extract(minute from systime)=minute(systime),'ok','nok');
select if(extract(minute from sys_time)=minute(sys_time),'ok','nok');

select if(extract(second from current_time)=second(current_time),'ok','nok');
select if(extract(second from current_time())=second(current_time()),'ok','nok');
select if(extract(second from curtime())=second(curtime()),'ok','nok');
select if(extract(second from systime)=second(systime),'ok','nok');
select if(extract(second from sys_time)=second(sys_time),'ok','nok');
set timezone 'Asia/Seoul';
