--+ server-message on

-- 


create or replace procedure p_local() as 
    procedure [AND] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'AND' ); 
    end; 
begin
    [AND](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [AS] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'AS' ); 
    end; 
begin
    [AS](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [BEGIN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'BEGIN' ); 
    end; 
begin
    [BEGIN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [BETWEEN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'BETWEEN' ); 
    end; 
begin
    [BETWEEN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [BIGINT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'BIGINT' ); 
    end; 
begin
    [BIGINT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [BOOLEAN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'BOOLEAN' ); 
    end; 
begin
    [BOOLEAN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [BY] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'BY' ); 
    end; 
begin
    [BY](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CASE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CASE' ); 
    end; 
begin
    [CASE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CHAR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CHAR' ); 
    end; 
begin
    [CHAR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CLOSE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CLOSE' ); 
    end; 
begin
    [CLOSE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [COMMIT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'COMMIT' ); 
    end; 
begin
    [COMMIT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CONSTANT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CONSTANT' ); 
    end; 
begin
    [CONSTANT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CONTINUE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CONTINUE' ); 
    end; 
begin
    [CONTINUE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CREATE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CREATE' ); 
    end; 
begin
    [CREATE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [CURSOR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'CURSOR' ); 
    end; 
begin
    [CURSOR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DATE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DATE' ); 
    end; 
begin
    [DATE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DATETIME] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DATETIME' ); 
    end; 
begin
    [DATETIME](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DATETIMELTZ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DATETIMELTZ' ); 
    end; 
begin
    [DATETIMELTZ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DATETIMETZ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DATETIMETZ' ); 
    end; 
begin
    [DATETIMETZ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DEC] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DEC' ); 
    end; 
begin
    [DEC](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DECIMAL] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DECIMAL' ); 
    end; 
begin
    [DECIMAL](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DECLARE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DECLARE' ); 
    end; 
begin
    [DECLARE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DEFAULT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DEFAULT' ); 
    end; 
begin
    [DEFAULT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DIV] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DIV' ); 
    end; 
begin
    [DIV](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [DOUBLE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'DOUBLE' ); 
    end; 
begin
    [DOUBLE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [ELSE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'ELSE' ); 
    end; 
begin
    [ELSE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [ELSIF] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'ELSIF' ); 
    end; 
begin
    [ELSIF](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [END] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'END' ); 
    end; 
begin
    [END](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [ESCAPE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'ESCAPE' ); 
    end; 
begin
    [ESCAPE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [EXCEPTION] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'EXCEPTION' ); 
    end; 
begin
    [EXCEPTION](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [EXECUTE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'EXECUTE' ); 
    end; 
begin
    [EXECUTE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [EXIT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'EXIT' ); 
    end; 
begin
    [EXIT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [FALSE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'FALSE' ); 
    end; 
begin
    [FALSE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [FETCH] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'FETCH' ); 
    end; 
begin
    [FETCH](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [FLOAT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'FLOAT' ); 
    end; 
begin
    [FLOAT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [FOR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'FOR' ); 
    end; 
begin
    [FOR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [FUNCTION] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'FUNCTION' ); 
    end; 
begin
    [FUNCTION](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [IF] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'IF' ); 
    end; 
begin
    [IF](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [IMMEDIATE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'IMMEDIATE' ); 
    end; 
begin
    [IMMEDIATE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [IN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'IN' ); 
    end; 
begin
    [IN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [INT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'INT' ); 
    end; 
begin
    [INT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [INTEGER] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'INTEGER' ); 
    end; 
begin
    [INTEGER](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [INTO] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'INTO' ); 
    end; 
begin
    [INTO](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [IS] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'IS' ); 
    end; 
begin
    [IS](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [LIKE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'LIKE' ); 
    end; 
begin
    [LIKE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [LIST] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'LIST' ); 
    end; 
begin
    [LIST](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [LOOP] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'LOOP' ); 
    end; 
begin
    [LOOP](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [MOD] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'MOD' ); 
    end; 
begin
    [MOD](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [MULTISET] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'MULTISET' ); 
    end; 
begin
    [MULTISET](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [NOT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'NOT' ); 
    end; 
begin
    [NOT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [NULL] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'NULL' ); 
    end; 
begin
    [NULL](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [NUMERIC] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'NUMERIC' ); 
    end; 
begin
    [NUMERIC](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [OPEN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'OPEN' ); 
    end; 
begin
    [OPEN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [OR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'OR' ); 
    end; 
begin
    [OR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [OUT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'OUT' ); 
    end; 
begin
    [OUT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [PRAGMA] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'PRAGMA' ); 
    end; 
begin
    [PRAGMA](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [PROCEDURE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'PROCEDURE' ); 
    end; 
begin
    [PROCEDURE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [RAISE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'RAISE' ); 
    end; 
begin
    [RAISE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [REAL] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'REAL' ); 
    end; 
begin
    [REAL](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [REPLACE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'REPLACE' ); 
    end; 
begin
    [REPLACE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [RETURN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'RETURN' ); 
    end; 
begin
    [RETURN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [REVERSE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'REVERSE' ); 
    end; 
begin
    [REVERSE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [ROLLBACK] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'ROLLBACK' ); 
    end; 
begin
    [ROLLBACK](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SEQUENCE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SEQUENCE' ); 
    end; 
begin
    [SEQUENCE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SET] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SET' ); 
    end; 
begin
    [SET](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SETEQ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SETEQ' ); 
    end; 
begin
    [SETEQ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SETNEQ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SETNEQ' ); 
    end; 
begin
    [SETNEQ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SHORT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SHORT' ); 
    end; 
begin
    [SHORT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SMALLINT] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SMALLINT' ); 
    end; 
begin
    [SMALLINT](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SQL] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SQL' ); 
    end; 
begin
    [SQL](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [STRING] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'STRING' ); 
    end; 
begin
    [STRING](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SUBSET] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SUBSET' ); 
    end; 
begin
    [SUBSET](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SUBSETEQ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SUBSETEQ' ); 
    end; 
begin
    [SUBSETEQ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SUPERSET] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SUPERSET' ); 
    end; 
begin
    [SUPERSET](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SUPERSETEQ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SUPERSETEQ' ); 
    end; 
begin
    [SUPERSETEQ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [SYS_REFCURSOR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'SYS_REFCURSOR' ); 
    end; 
begin
    [SYS_REFCURSOR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [THEN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'THEN' ); 
    end; 
begin
    [THEN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [TIME] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'TIME' ); 
    end; 
begin
    [TIME](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [TIMESTAMP] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'TIMESTAMP' ); 
    end; 
begin
    [TIMESTAMP](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [TIMESTAMPLTZ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'TIMESTAMPLTZ' ); 
    end; 
begin
    [TIMESTAMPLTZ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [TIMESTAMPTZ] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'TIMESTAMPTZ' ); 
    end; 
begin
    [TIMESTAMPTZ](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [TRUE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'TRUE' ); 
    end; 
begin
    [TRUE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [USING] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'USING' ); 
    end; 
begin
    [USING](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [VARCHAR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'VARCHAR' ); 
    end; 
begin
    [VARCHAR](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [WHEN] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'WHEN' ); 
    end; 
begin
    [WHEN](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [WHILE] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'WHILE' ); 
    end; 
begin
    [WHILE](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [WORK] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'WORK' ); 
    end; 
begin
    [WORK](); 
end;
call p_local();

create or replace procedure p_local() as 
    procedure [XOR] as 
    begin 
        dbms_output.put_line('call local_procedure.' || 'XOR' ); 
    end; 
begin
    [XOR](); 
end;
call p_local();

drop procedure p_local;

--+ server-message on

