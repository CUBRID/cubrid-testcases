===================================================
0
===================================================
0
===================================================
0
===================================================
2
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a
===================================================
0
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(+)
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(+)
===================================================
0
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_ab term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(-)
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_ab term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(-)
===================================================
0
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a
===================================================
0
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(+)
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(+)
===================================================
0
===================================================
0
===================================================
a    b    c    d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_ab term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(-)
===================================================
a    b    c    d    
2     2     2     2     

Query plan:
iscan
    class: t? node[?]
    index: i?_ab term[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c, t?.d from t? t? where (t?.a> ?:? ) using index t?.i?_a(-)
===================================================
0
===================================================
0
