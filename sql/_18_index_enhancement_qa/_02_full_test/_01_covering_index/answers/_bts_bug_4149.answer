===================================================
0
===================================================
1
===================================================
1
===================================================
1
===================================================
a    b    c    
1     1     1     
null     null     null     
null     1     1     

Query plan:
sscan
    class: t? node[?]
    cost:  ? card ?
Query stmt:
select t?.a, t?.b, t?.c from t? t?
===================================================
count(1)    
3     

Query plan:
sscan
    class: t? node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select count(*) from t? t? where (t?.a is not null  or t?.a is null )
===================================================
0
===================================================
0
===================================================
count(1)    
3     

Query plan:
sscan
    class: t? node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select count(*) from t? t? where (t?.a is not null  or t?.a is null )
===================================================
0
===================================================
0
