===================================================
0
===================================================
0
===================================================
0
===================================================
1
===================================================
1
===================================================
1
===================================================
1
===================================================
id    v    
3     dbms qa2 part     
4     dbms qa2 part     

Query plan:
temp(order by)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  ? asc
    cost:  ? card ?
Query stmt:
select t?.id, t?.v from t? t? where t?.v not like 'dbms qa?%' collate iso?_bin_ts order by ?
===================================================
id    v    
1     dbms qa1 part     
2     dbms qa1 part     

Query plan:
temp(order by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_v term[?]
                 sort:  ? asc
                 cost:  ? card ?
    sort:  ? asc
    cost:  ? card ?
Query stmt:
select t?.id, t?.v from t? t? where (t?.v>= ?:?  and t?.v< ?:? ) order by ?
===================================================
0
===================================================
0
===================================================
0
