===================================================
0
===================================================
0
===================================================
2
===================================================
bar    
a     
null     

Query plan:
temp(order by)
    subplan: sscan
                 class: foo node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  ? desc
    cost:  ? card ?
Query stmt:
select foo.bar from foo foo where (foo.bar is null  or foo.bar is not null ) order by ? desc 
===================================================
0
===================================================
0
===================================================
0
