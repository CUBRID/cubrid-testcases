===================================================
0
===================================================
0
===================================================
1
===================================================
0
===================================================
count( char_length(a))    10    
1     10     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_a?a term[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
(select t?.a from t? t? where ( lower(t?.b)> ?:? ) and inst_num()<= ?:? )
Query plan:
sscan
    class: t? node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select count( char_length(t?.a)), ? from t? t? where  exists (select t?.a from t? t? where ( lower(t?.b)> ?:? ) and inst_num()<= ?:? )
===================================================
count( char_length(a))    10    
1     10     

Query plan:
sscan
    class: dual node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
(select t?.a from dual dual where inst_num()<=?)
Query plan:
iscan
    class: t? node[?]
    index: i_t?_a?a term[?]
    sargs: term[?]
    subqs: ?
    cost:  ? card ?
Query stmt:
select count( char_length(t?.a)), ? from t? t? where  exists (select t?.a from dual dual where inst_num()<=?) and ( lower(t?.b)> ?:? )
===================================================
count( char_length(a))    10    
1     10     

Query plan:
sscan
    class: dual node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
(select t?.a from dual dual where inst_num()<=?)
Query plan:
sscan
    class: t? node[?]
    sargs: term[?]
    subqs: ?
    cost:  ? card ?
Query stmt:
select count( char_length(t?.a)), ? from t? t? where ( exists (select t?.a from dual dual where inst_num()<=?) or ( lower(t?.b)> ?:? ))
===================================================
0
===================================================
0
===================================================
0
