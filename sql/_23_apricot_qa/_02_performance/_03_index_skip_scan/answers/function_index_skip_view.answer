===================================================
0
===================================================
0
===================================================
0
===================================================
10000
===================================================
0
===================================================
0
===================================================
0
===================================================
0
===================================================
0
===================================================
i    j    k    l    
1     1     1     1     

Query plan:
iscan
    class: t_view? node[?]
    index: idx term[?] (index skip scan)
    sort:  ? asc, ? asc
    cost:  ? card ?
Query stmt:
select /*+ INDEX_SS */ t_view?.i, t_view?.j, t_view?.k, t_view?.l from t t_view? where (t_view?.j= ?:? ) order by ?
/* ---> skip ORDER BY */
===================================================
i    j    k    l    
1     1     1     1     

Query plan:
iscan
    class: t_view? node[?]
    index: idx term[?] (index skip scan)
    sort:  ? asc, ? asc, ? asc
    cost:  ? card ?
Query stmt:
select /*+ INDEX_SS */ t_view?.i, t_view?.j, t_view?.k, t_view?.l from t t_view? where (t_view?.j= any {?}) order by ?
/* ---> skip ORDER BY */
===================================================
0
===================================================
0
===================================================
0
===================================================
0
