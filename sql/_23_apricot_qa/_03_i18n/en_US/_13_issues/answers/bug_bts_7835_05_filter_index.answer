===================================================
0
===================================================
0
===================================================
0
===================================================
1
===================================================
1
===================================================
1
===================================================
s1    
accİ     
aİbc     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate(_utf?'İ' collate utf?_en_cs, t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
0
===================================================
s1    

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate( cast('İ' as char(-?) collate utf?_en_cs), t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
s1    
accİ     
aİbc     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate(_utf?'İ' collate utf?_en_cs, t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
0
===================================================
0
===================================================
0
===================================================
0
===================================================
1
===================================================
1
===================================================
1
===================================================
s1    
accİ     
aİbc     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate(_utf?'İ' collate utf?_en_cs, t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
0
===================================================
s1    

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate( cast('İ' as char(-?) collate utf?_en_cs), t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
s1    
accİ     
aİbc     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_f_s? (covers)
    filtr: term[?]
    sort:  ? asc collate utf?_en_cs
    cost:  ? card ?
Query stmt:
select t?.s? from t? t? where  locate(_utf?'İ' collate utf?_en_cs, t?.s?)>? using index t?.i_t?_f_s?(+) order by ?
/* ---> skip ORDER BY */
===================================================
0
===================================================
0
===================================================
0
