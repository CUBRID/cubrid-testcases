===================================================
0
===================================================
1000
===================================================
1
===================================================
1
===================================================
1
===================================================
1
===================================================
1
===================================================
0
===================================================
a    
y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_ci
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_ci
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
upper(a)    
Y     

Query plan:
temp(group by)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */  upper(t?.a) from t? t? where  cast(t?.a as varchar(?) collate iso?_en_ci)='Y' collate iso?_en_ci group by t?.a collate iso?_en_ci
===================================================
a    
Y     
y     

Query plan:
temp(group by)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where  cast(t?.a as varchar(?) collate iso?_en_ci)='Y' collate iso?_en_ci group by t?.a collate iso?_en_cs
===================================================
upper(a)    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */  upper(t?.a) from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_ci
===================================================
a    
y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
upper(a)    
Y     

Query plan:
temp(group by)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */  upper(t?.a) from t? t? where  cast(t?.a as varchar(?) collate iso?_en_ci)='y' collate iso?_en_ci group by t?.a collate iso?_en_ci
===================================================
a    
Y     
y     

Query plan:
temp(group by)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where  cast(t?.a as varchar(?) collate iso?_en_ci)='y' collate iso?_en_ci group by t?.a collate iso?_en_cs
===================================================
a    
y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all term[?] (covers) (loose index scan on prefix ?)
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where t?.a= ?:?  group by t?.a collate iso?_en_cs
===================================================
a    
Y     
y     

Query plan:
iscan
    class: t? node[?]
    index: i_t?_all (covers) (loose index scan on prefix ?)
    filtr: term[?]
    sort:  ? asc collate iso?_en_cs
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where (t?.c> ?:? ) group by t?.a
/* ---> skip GROUP BY */
===================================================
upper(a)    
Y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */  upper(t?.a) from t? t? where (t?.c> ?:? ) group by t?.a collate iso?_en_ci
===================================================
a    
Y     
y     

Query plan:
temp(group by)
    subplan: iscan
                 class: t? node[?]
                 index: i_t?_all (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 sort:  ? asc collate iso?_en_cs
                 cost:  ? card ?
    sort:  
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ t?.a from t? t? where (t?.c> ?:? ) group by t?.a collate iso?_en_cs
===================================================
0
