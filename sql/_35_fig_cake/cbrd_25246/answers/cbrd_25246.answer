===================================================
0
===================================================
0
===================================================
100
===================================================
if(count(*)>0 and min(p_cur_volumeid)>=0 and min(p_cur_pageid)>=0, 'TRUE', 'FALSE')    
TRUE     

===================================================
0
