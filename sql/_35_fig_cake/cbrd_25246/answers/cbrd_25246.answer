===================================================
0
===================================================
0
===================================================
100
===================================================
if(count(*)>0 and count(p_cur_volumeid)=count(*) and count(p_cur_pageid)=count(*) and min(p_cur_volumeid)>=0 and min(p_cur_pageid)>=0, 'TRUE', 'FALSE')    
TRUE     

===================================================
0
