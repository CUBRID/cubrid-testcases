===================================================
0
0
===================================================
1
===================================================
0
===================================================
a    
1     

Query plan:
sscan
    class: t? node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t?.a from t? t? where  cast( trim(both  from t?.a) as double)=?
===================================================
a    
1     

Query plan:
iscan
    class: t? node[?]
    index: t?_a term[?]
    cost:  ? card ?
Query stmt:
select t?.a from t? t? where  trim(both  from t?.a)= ?:? 
===================================================
0
===================================================
0
===================================================
1
===================================================
0
===================================================
a    
1     

Query plan:
iscan
    class: t? node[?]
    index: t?_a term[?] (covers)
    cost:  ? card ?
Query stmt:
select t?.a from t? t? where t?.a= ?:? 
===================================================
0
===================================================
0
