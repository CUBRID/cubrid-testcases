===================================================
0
===================================================
0
===================================================
1
===================================================
@v1 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v2 := sum(b) over (partition by a order by b)    
1     

===================================================
@v3 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v3 := max(b) over (partition by a order by b)    
1     

===================================================
@v3 := max(b) over (partition by a order by b+b)    
1     

===================================================
@v3 := max(b) over (partition by a+a order by b+b)    
1     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := count(*) over (partition by a+a order by b+b)    
1     

===================================================
0
===================================================
0
===================================================
1
===================================================
@v1 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v2 := sum(b) over (partition by a order by b)    
1.0     

===================================================
@v3 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v3 := max(b) over (partition by a order by b)    
1     

===================================================
@v3 := max(b) over (partition by a order by b+b)    
1     

===================================================
@v3 := max(b) over (partition by a+a order by b+b)    
1     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := count(*) over (partition by a+a order by b+b)    
1     

===================================================
0
===================================================
0
===================================================
1
===================================================
@v1 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v2 := sum(b) over (partition by a order by b)    
1     

===================================================
@v3 := avg(b) over (partition by a order by b)    
1.0     

===================================================
@v3 := max(b) over (partition by a order by b)    
1     

===================================================
@v3 := max(b) over (partition by a order by b+b)    
1     

===================================================
@v3 := max(b) over (partition by a+a order by b+b)    
1     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := avg(b) over (partition by a+a order by b+b)    
1.0     

===================================================
@v3 := count(*) over (partition by a+a order by b+b)    
1     

===================================================
@v3 := count(b) over (partition by a+a order by b+b)    
1     

===================================================
@v3 := count(b) over (partition by a+a order by b+b)    
1     

===================================================
0
===================================================
0
===================================================
0
===================================================
0
