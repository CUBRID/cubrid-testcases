drop table if exists t;

create table t
	(
	    id		    integer unique PRIMARY KEY,
	    textlabel	    varchar(255),
	    description	    varchar(4096)
	);

drop t;

create table t
	(
	    id		    integer unique PRIMARY KEY,
	    textlabel	    varchar(255),
	    description	    varchar(4096)
	)
	;

drop t;

create table t
	(
	  id		    integer unique PRIMARY KEY,
	    textlabel		  varchar(255),
	    description		  varchar(4096)
	);

drop t;

create table t
	(
	    identit     integer unique PRIMARY KEY,
	    identi	 integer  ,
	    textlabel		  varchar(255),
	    description		  varchar(4096)
	);

drop t;

create table t
	(
	    pri		    integer unique PRIMARY KEY,
	    textlabel	    varchar(255),
	    description	    varchar(4096)
	);
drop t;
