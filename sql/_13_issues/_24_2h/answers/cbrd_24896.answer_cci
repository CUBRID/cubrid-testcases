===================================================
0
===================================================
0
===================================================
0
===================================================
4
===================================================
    
brace set term must not be used as an index key     

===================================================
a    b    c    
1     2     x     

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b} seteq {?, ?} using index t_tbl.i_tbl_ab
===================================================
a    b    c    
1     2     x     

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b}={?, ?} using index t_tbl.i_tbl_ab
===================================================
    
rhs longer than lhs must not return a wrong result     

===================================================
a    b    c    

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b}={?, ?, ?} using index t_tbl.i_tbl_ab
===================================================
    
rhs shorter than lhs must not crash     

===================================================
a    b    c    

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b} seteq {} using index t_tbl.i_tbl_ab
===================================================
a    b    c    

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b}={} using index t_tbl.i_tbl_ab
===================================================
a    b    c    

Query plan:
sscan
    class: t_tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where {t_tbl.a, t_tbl.b}={?} using index t_tbl.i_tbl_ab
===================================================
    
paren tuple term is still indexable     

===================================================
a    b    c    
1     2     x     

Query plan:
iscan
    class: t_tbl node[?]
    index: i_tbl_ab term[?] AND term[?]
    cost:  ? card ?
Query stmt:
select t_tbl.a, t_tbl.b, t_tbl.c from t_tbl t_tbl where t_tbl.b= ?:?  and t_tbl.a= ?:?  using index t_tbl.i_tbl_ab
===================================================
    
system table without join     

===================================================
name    
PUBLIC     

Query plan:
sscan
    class: u node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select u.[name] from [_db_user] u where {u.[name]} seteq {'PUBLIC'} using index u.u__db_user_name
===================================================
name    
PUBLIC     

Query plan:
sscan
    class: u node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select u.[name] from [_db_user] u where {u.[name]}={'PUBLIC'} using index u.u__db_user_name
===================================================
name    

Query plan:
sscan
    class: u node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select u.[name] from [_db_user] u where {u.[name]}={'PUBLIC', ?} using index u.u__db_user_name
===================================================
name    

Query plan:
sscan
    class: u node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select u.[name] from [_db_user] u where {u.[name]}={} using index u.u__db_user_name
===================================================
0
===================================================
0
===================================================
0
===================================================
3
===================================================
0
===================================================
0
===================================================
    
index scan case     

===================================================
col_a    col_b    col_c    col_d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: tbl node[?]
    index: idx term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where (({tbl.col_a, tbl.col_b}={?, ?}) or ({tbl.col_a, tbl.col_b}={?, ?}))
===================================================
col_a    col_b    col_c    col_d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: tbl node[?]
    index: idx term[?]
    filtr: term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where (({tbl.col_a, tbl.col_c}={?, ?}) or ({tbl.col_a, tbl.col_c}={?, ?}))
===================================================
col_a    col_b    col_c    col_d    
1     1     1     1     
2     2     2     2     

Query plan:
iscan
    class: tbl node[?]
    index: idx term[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where (({tbl.col_a, tbl.col_d}={?, ?}) or ({tbl.col_a, tbl.col_d}={?, ?}))
===================================================
    
sscan case     

===================================================
col_a    col_b    col_c    col_d    
1     1     1     1     
2     2     2     2     

Query plan:
sscan
    class: tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where (({tbl.col_a, tbl.col_b}={?, ?}) or ({tbl.col_a, tbl.col_b}={?, ?}))
===================================================
col_a    col_b    col_c    col_d    
2     2     2     2     

Query plan:
sscan
    class: tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where {tbl.col_a}={?}
===================================================
col_a    col_b    col_c    col_d    

Query plan:
sscan
    class: tbl node[?]
    sargs: term[?]
    cost:  ? card ?
Query stmt:
select tbl.col_a, tbl.col_b, tbl.col_c, tbl.col_d from tbl tbl where {tbl.col_a}={}
===================================================
0
