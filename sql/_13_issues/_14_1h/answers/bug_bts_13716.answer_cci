===================================================
0
===================================================
0
===================================================
1000
===================================================
b    
1     

===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: t? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?) */ distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: t? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: t? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?) */ distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: t? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: t? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?) */ distinct t?.b from t? t? where (t?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: x? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(x?) */ distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: x? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: x? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS */ distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: x? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(x?) */ distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: x? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: x? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: sscan
                 class: x? node[?]
                 sargs: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: x? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(x?) */ distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: x? node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(x?) */ distinct x?.b from t? x? where (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: iscan
                 class: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx node[?]
                 index: i_abc (covers) (loose index scan on prefix ?)
                 filtr: term[?]
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx) */ distinct xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.b from t? xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx where (xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.b< ?:? ) for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: nl-join (inner join)
                 edge:  term[?]
                 outer: iscan
                            class: t? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?]
                            cost:  ? card ?
                 inner: iscan
                            class: x? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?] AND term[?]
                            cost:  ? card ?
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?, x?) */ distinct t?.b from t? t?, t? x? where (t?.b< ?:? ) and (x?.b< ?:? ) and t?.b=x?.b for orderby_num()<= ?:? 
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: nl-join (inner join)
                 edge:  term[?]
                 outer: iscan
                            class: t? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?]
                            cost:  ? card ?
                 inner: iscan
                            class: x? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?] AND term[?]
                            cost:  ? card ?
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?, x?) */ distinct t?.b from t? t?, t? x? where (t?.b< ?:? ) and (x?.b< ?:? ) and t?.b=x?.b for orderby_num()<= ?:? 
===================================================
0
===================================================
b    
1     

Query plan:
temp(distinct)
    subplan: nl-join (cross join)
                 outer: iscan
                            class: t? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?]
                            cost:  ? card ?
                 inner: iscan
                            class: x? node[?]
                            index: i_abc (covers) (loose index scan on prefix ?)
                            filtr: term[?]
                            cost:  ? card ?
                 cost:  ? card ?
    cost:  ? card ?
Query stmt:
select /*+ INDEX_LS(t?, x?) */ distinct t?.b from t? t?, t? x? where (t?.b< ?:? ) and (x?.b< ?:? ) for orderby_num()<= ?:? 
===================================================
0
===================================================
0
